# pmettf

An incomplete Python script for converting the single palm memo export dump to individual text files.

Files are dumped to a currently hard-coded "converted" directory.  If this directly already exists then it is deleted before rewriting.
